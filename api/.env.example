# Example environment variables for the Element API
# Copy this file to `.env` and fill in the real values. Do NOT commit real secrets.

DB_HOST=your-db-host.example.com
DB_PORT=3306
DB_NAME=u949019802_element
DB_USER=u949019802_dv8
DB_PASS=REPLACE_WITH_PASSWORD

# Optional: port the API should listen on (default 3000)
PORT=3000
